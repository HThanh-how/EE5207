\chapter{Processor Architecture}

\section{Overview}

The project progresses from a \textbf{single-cycle} RV32I processor (Milestone~2) to a
\textbf{five-stage pipelined} RV32I processor (Milestone~3). The single-cycle core serves
as the functional baseline, while the pipelined core reuses the same ISA and I/O conventions
but introduces pipeline stages, hazard detection, and forwarding to improve throughput.

Unless otherwise stated, this chapter focuses on the Milestone~3 pipelined architecture.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.85\textwidth]{docs/milestone-3/figure1.png}
    \caption{Pipelined processor with hazard detection, forwarding, and branch signals (\texttt{o\_ctrl}, \texttt{o\_mispred}).}
    \label{fig:pipeline_overview}
\end{figure}

\section{Milestone 3 Pipelined Overview}

\begin{figure}[h]
    \centering
    \begin{tikzpicture}[
        stage/.style={rectangle, draw, rounded corners, minimum width=2.0cm, minimum height=1.0cm, fill=blue!8},
        reg/.style={rectangle, draw, minimum width=1.5cm, minimum height=0.8cm, fill=orange!10},
        arrow/.style={->, thick}
    ]
        \node[stage] (if) {IF};
        \node[reg, right=0.9cm of if] (ifid) {IF/ID};
        \node[stage, right=0.9cm of ifid] (id) {ID};
        \node[reg, right=0.9cm of id] (idex) {ID/EX};
        \node[stage, right=0.9cm of idex] (ex) {EX};
        \node[reg, right=0.9cm of ex] (exmem) {EX/MEM};
        \node[stage, right=0.9cm of exmem] (mem) {MEM};
        \node[reg, right=0.9cm of mem] (memwb) {MEM/WB};
        \node[stage, right=0.9cm of memwb] (wb) {WB};

        \draw[arrow] (if) -- (ifid);
        \draw[arrow] (ifid) -- (id);
        \draw[arrow] (id) -- (idex);
        \draw[arrow] (idex) -- (ex);
        \draw[arrow] (ex) -- (exmem);
        \draw[arrow] (exmem) -- (mem);
        \draw[arrow] (mem) -- (memwb);
        \draw[arrow] (memwb) -- (wb);

        \draw[arrow, bend right=25] (exmem) to node[below]{fwd} (idex);
        \draw[arrow, bend right=35] (memwb) to node[below]{fwd} (idex);
        \draw[arrow, bend left=30, red] (ex) to node[above]{flush/pc\_corr} (if);
        \draw[arrow, bend left=35, dashed] (if) to node[above]{pred PC} (ex);
    \end{tikzpicture}
    \caption{Luồng 5 stage, forwarding (fwd), flush khi mispredict, PC correction.}
    \label{fig:pipeline_tikz}
\end{figure}

\section{Block Diagram of Pipelined Processor}

\begin{figure}[h]
    \centering
    \resizebox{\textwidth}{!}{%
    \begin{tikzpicture}[
        >=stealth,
        node distance=1.5cm and 2cm,
        stage/.style={rectangle, draw, rounded corners, minimum width=2.5cm, minimum height=1.5cm, fill=blue!10, text centered},
        module/.style={rectangle, draw, rounded corners, minimum width=2cm, minimum height=1cm, fill=green!10, text centered},
        reg/.style={rectangle, draw, minimum width=1.8cm, minimum height=0.8cm, fill=orange!15, text centered, font=\small},
        mux/.style={rectangle, draw, minimum width=0.8cm, minimum height=0.8cm, fill=yellow!20},
        unit/.style={rectangle, draw, rounded corners, minimum width=2cm, minimum height=1cm, fill=purple!10, text centered, font=\small},
        signal/.style={->, thick},
        control/.style={->, dashed, red, thick},
        forward/.style={->, dashed, blue, thick}
    ]
        % IF Stage
        \node[stage] (if_stage) {IF Stage};
        \node[module, below=0.5cm of if_stage] (imem) {IMEM\\Sync};
        \node[unit, right=1cm of if_stage] (btb) {BTB\\256 entries};
        \node[unit, above=0.5cm of if_stage] (pc) {PC};
        
        % IF/ID Register
        \node[reg, right=1.5cm of if_stage] (ifid) {IF/ID\\Reg};
        
        % ID Stage
        \node[stage, right=1.5cm of ifid] (id_stage) {ID Stage};
        \node[module, below=0.5cm of id_stage] (control) {Control\\Unit};
        \node[module, below=0.5cm of control] (regfile) {Register\\File};
        \node[module, below=0.5cm of regfile] (imm_gen) {Immediate\\Generator};
        
        % ID/EX Register
        \node[reg, right=1.5cm of id_stage] (idex) {ID/EX\\Reg};
        
        % EX Stage
        \node[stage, right=1.5cm of idex] (ex_stage) {EX Stage};
        \node[module, below=0.5cm of ex_stage] (alu) {ALU};
        \node[unit, left=1cm of ex_stage] (forward_unit) {Forwarding\\Unit};
        \node[unit, above=0.5cm of ex_stage] (branch_comp) {Branch\\Compare};
        
        % EX/MEM Register
        \node[reg, right=1.5cm of ex_stage] (exmem) {EX/MEM\\Reg};
        
        % MEM Stage
        \node[stage, right=1.5cm of exmem] (mem_stage) {MEM Stage};
        \node[module, below=0.5cm of mem_stage] (dmem) {DMEM\\Sync};
        \node[module, below=0.5cm of dmem] (io_map) {I/O\\Mapping};
        
        % MEM/WB Register
        \node[reg, right=1.5cm of mem_stage] (memwb) {MEM/WB\\Reg};
        
        % WB Stage
        \node[stage, right=1.5cm of memwb] (wb_stage) {WB Stage};
        \node[mux, below=0.5cm of wb_stage] (wb_mux) {MUX};
        
        % Hazard Detection Unit
        \node[unit, above=2cm of ex_stage] (hazard) {Hazard\\Detection};
        
        % Main pipeline flow
        \draw[signal] (if_stage) -- (ifid);
        \draw[signal] (ifid) -- (id_stage);
        \draw[signal] (id_stage) -- (idex);
        \draw[signal] (idex) -- (ex_stage);
        \draw[signal] (ex_stage) -- (exmem);
        \draw[signal] (exmem) -- (mem_stage);
        \draw[signal] (mem_stage) -- (memwb);
        \draw[signal] (memwb) -- (wb_stage);
        \draw[signal] (wb_stage) -- (wb_mux);
        
        % PC connections
        \draw[signal] (pc) -- (if_stage);
        \draw[signal] (if_stage) -- (imem);
        \draw[signal] (imem) -- (ifid);
        \draw[signal, bend left=30] (ex_stage) to node[above, font=\tiny] {corrected\_pc} (pc);
        
        % BTB connections
        \draw[signal, dashed] (if_stage) -- (btb);
        \draw[signal, dashed] (btb) -- (if_stage);
        \draw[signal, dashed, bend left=20] (ex_stage) to node[above, font=\tiny] {update} (btb);
        
        % Control Unit connections
        \draw[signal] (ifid) -- (control);
        \draw[control] (control) -- (idex);
        \draw[control] (control) -- (regfile);
        
        % Register File connections
        \draw[signal] (ifid) -- (regfile);
        \draw[signal] (regfile) -- (idex);
        \draw[signal] (wb_mux) -- (regfile);
        
        % Immediate Generator
        \draw[signal] (ifid) -- (imm_gen);
        \draw[signal] (imm_gen) -- (idex);
        
        % Forwarding connections
        \draw[forward] (exmem) to[bend right=30] node[below, font=\tiny] {mem\_alu\_result} (forward_unit);
        \draw[forward] (memwb) to[bend right=40] node[below, font=\tiny] {wb\_reg\_wdata} (forward_unit);
        \draw[forward] (forward_unit) -- (ex_stage);
        
        % ALU connections
        \draw[signal] (idex) -- (alu);
        \draw[signal] (alu) -- (exmem);
        
        % Branch comparison
        \draw[signal] (idex) -- (branch_comp);
        \draw[signal] (branch_comp) -- (ex_stage);
        
        % DMEM connections
        \draw[signal] (exmem) -- (dmem);
        \draw[signal] (dmem) -- (memwb);
        \draw[signal] (exmem) -- (io_map);
        \draw[signal] (io_map) -- (memwb);
        
        % Hazard Detection connections
        \draw[control] (idex) -- (hazard);
        \draw[control] (exmem) -- (hazard);
        \draw[control] (hazard) to[bend right=30] node[left, font=\tiny] {stall/flush} (if_stage);
        \draw[control] (hazard) to[bend right=20] node[left, font=\tiny] {stall/flush} (id_stage);
        \draw[control] (hazard) to[bend left=20] node[right, font=\tiny] {flush} (ex_stage);
        
        % Writeback MUX
        \draw[signal] (memwb) -- (wb_mux);
        \draw[signal] (exmem) to[bend right=50] node[below, font=\tiny] {alu\_result} (wb_mux);
        \draw[signal] (memwb) to[bend left=30] node[above, font=\tiny] {pc+4} (wb_mux);
        
    \end{tikzpicture}%
    }
    \caption{Complete block diagram of the five-stage pipelined RISC-V processor showing all major components, pipeline registers, forwarding paths, hazard detection, and branch prediction.}
    \label{fig:pipeline_block_diagram}
\end{figure}

\subsection{Component Descriptions}

The block diagram in Figure~\ref{fig:pipeline_block_diagram} illustrates the complete architecture of the pipelined processor:

\begin{itemize}
    \item \textbf{IF Stage}: Fetches instructions from IMEM using PC. BTB provides predicted PC for branch/jump instructions.
    \item \textbf{ID Stage}: Decodes instruction using Control Unit, reads registers from Register File, and generates immediate values.
    \item \textbf{EX Stage}: Executes ALU operations, performs branch comparison, and calculates branch/jump targets. Forwarding Unit resolves data hazards.
    \item \textbf{MEM Stage}: Accesses DMEM for load/store operations and handles I/O memory mapping.
    \item \textbf{WB Stage}: Writes results back to Register File via Writeback MUX.
    \item \textbf{Pipeline Registers}: IF/ID, ID/EX, EX/MEM, MEM/WB store intermediate values between stages.
    \item \textbf{Forwarding Unit}: Forwards data from MEM and WB stages to EX stage to resolve data hazards.
    \item \textbf{Hazard Detection Unit}: Detects load-use hazards and control hazards, generates stall and flush signals.
    \item \textbf{Branch Target Buffer (BTB)}: 256-entry buffer storing branch/jump targets and two-bit predictor states.
\end{itemize}

\section{Datapath Components}

\subsection{Program Counter (PC)}

The Program Counter tracks the current instruction address and updates based on instruction type:

\begin{itemize}
    \item \textbf{Sequential}: PC = PC + 4 (default for non-branch/jump instructions)
    \item \textbf{Branch}: PC = PC + immediate (if branch condition is true)
    \item \textbf{Jump}: PC = PC + immediate (JAL) or rs1 + immediate (JALR)
    \item \textbf{Reset}: When \texttt{i\_reset} is LOW (active), PC is reset to 0x0000\_0000
\end{itemize}

The PC register updates on the rising edge of the clock. When \texttt{i\_reset} is LOW, PC is synchronously reset to 0x0000\_0000. When \texttt{i\_reset} is HIGH (not reset), PC updates to \texttt{pc\_next}.

\subsection{Instruction Memory (IMEM)}

The instruction memory is implemented as a 16KB read-only array:

\begin{itemize}
    \item \textbf{Implementation}: Logic element array (16,384 bytes)
    \item \textbf{Addressing}: Byte-addressable, but fetches 32-bit words
    \item \textbf{Access}: Combinational read (asynchronous)
    \item \textbf{Initialization}: Pre-loaded from \texttt{isa.mem} file (with fallback to \texttt{isa\_1b.hex} for local testing)
\end{itemize}

\subsection{Instruction Decode}

The instruction is decoded into the following fields:

\begin{itemize}
    \item \textbf{opcode}: bits [6:0] - Instruction type identifier
    \item \textbf{rd}: bits [11:7] - Destination register
    \item \textbf{funct3}: bits [14:12] - Function code for opcode variants
    \item \textbf{rs1}: bits [19:15] - Source register 1
    \item \textbf{rs2}: bits [24:20] - Source register 2
    \item \textbf{funct7}: bits [31:25] - Additional function code (for R-type)
\end{itemize}

\subsection{Branch Prediction and BTB}

The pipelined core adds a two-bit dynamic branch predictor with a 256-entry BTB. Lookup
occurs in IF; update happens in EX when the branch outcome is known. The default prediction
is not-taken; entries transition between four states (00--11) based on correctness.

\begin{lstlisting}[language=Verilog, caption=BTB lookup and prediction (pipelined.sv)]
// Index and tag from PC
assign btb_index_if = pc[BTB_INDEX_WIDTH+1:2];
assign btb_tag_if   = pc[31:BTB_INDEX_WIDTH+2];

always_comb begin
    btb_hit       = 1'b0;
    predicted_pc  = pc_plus4; // default: not-taken
    predict_taken = 1'b0;

    if (btb_tag[btb_index_if] == btb_tag_if) begin
        if (btb_state[btb_index_if] >= 2'b10) begin
            btb_hit       = 1'b1;
            predicted_pc  = btb_target[btb_index_if];
            predict_taken = 1'b1;
        end
    end
end
\end{lstlisting}

On misprediction (detected in EX), IF/ID/EX are flushed and PC is corrected to the actual
target. The signal \texttt{o\_mispred} counts mispredictions in WB; \texttt{o\_ctrl} counts control
transfers in WB for IPC and mispred rate.

\subsection{Pipeline Registers (Enable/Flush)}

\begin{lstlisting}[language=Verilog, caption=Pipeline registers with enable/flush]
// IF/ID
always_ff @(posedge i_clk) begin
  if (~i_reset || if_flush) begin
    id_pc <= '0; id_instruction <= '0; id_enable <= 1'b0;
  end else if (if_enable && ~stall_id) begin
    id_pc <= pc; id_instruction <= if_instruction; id_enable <= 1'b1;
  end
end

// ID/EX
always_ff @(posedge i_clk) begin
  if (~i_reset || flush_id) begin
    ex_enable <= 1'b0; ex_predicted_taken <= 1'b0;
  end else if (~stall_ex && id_enable) begin
    ex_enable <= id_enable;
    ex_predicted_taken <= (id_branch || id_jump) ? predict_taken : 1'b0;
  end
end
\end{lstlisting}

\subsection{Forwarding \& Hazard Control}

\begin{lstlisting}[language=Verilog, caption=Forwarding and load-use stall]
// Forwarding priority: MEM > WB
if (mem_reg_write && mem_enable && mem_rd_addr != 5'b0) begin
  if (mem_rd_addr == ex_rs1_addr) forward_a = 2'b10;
  if (mem_rd_addr == ex_rs2_addr) forward_b = 2'b10;
end
if (wb_reg_write && wb_enable && wb_rd_addr != 5'b0) begin
  if (forward_a == 2'b00 && wb_rd_addr == ex_rs1_addr) forward_a = 2'b01;
  if (forward_b == 2'b00 && wb_rd_addr == ex_rs2_addr) forward_b = 2'b01;
end

// Load-use hazard stall
if (ex_is_load && ex_enable && ex_rd_addr != 5'b0) begin
  if ((id_rs1_addr == ex_rd_addr && id_reg_write) ||
      (id_rs2_addr == ex_rd_addr && (id_mem_write || id_branch))) begin
    stall_if = 1'b1; stall_id = 1'b1; flush_ex = 1'b1;
  end
end
\end{lstlisting}

\subsection{Misprediction Handling}

\begin{lstlisting}[language=Verilog, caption=Misprediction flush and PC correction]
// Detect misprediction in EX
if (ex_is_ctrl && ex_enable) begin
  logic actual_taken;
  logic [31:0] actual_target;
  actual_taken  = ex_branch ? ex_branch_taken : 1'b1;
  actual_target = ex_branch ? ex_branch_target : ex_jump_target;

  if (ex_predicted_taken != actual_taken) begin
    flush_if = 1'b1; flush_id = 1'b1; flush_ex = 1'b1;
    pc_next  = actual_target; // corrected PC
  end
end
\end{lstlisting}

\subsection{Immediate Generation}

The immediate generator extracts and sign-extends immediate values based on instruction type:

\begin{itemize}
    \item \textbf{I-type}: Sign-extend bits [31:20]
    \item \textbf{S-type}: Sign-extend bits [31:25, 11:7]
    \item \textbf{B-type}: Sign-extend bits [31, 7, 30:25, 11:8], LSB = 0
    \item \textbf{U-type}: bits [31:12] << 12 (LUI, AUIPC)
    \item \textbf{J-type}: Sign-extend bits [31, 19:12, 20, 30:21], LSB = 0
\end{itemize}

\subsection{Register File}

The register file provides two read ports and one write port:

\begin{itemize}
    \item \textbf{Size}: 32 registers, each 32 bits wide
    \item \textbf{Read}: Asynchronous (combinational) - data available immediately
    \item \textbf{Write}: Synchronous (on clock edge) - only when \texttt{reg\_write} is enabled
    \item \textbf{Register x0}: Hardwired to zero (always reads 0, writes ignored)
    \item \textbf{Read Ports}: rs1 and rs2 addresses from instruction fields
    \item \textbf{Write Port}: rd address, data from writeback multiplexer
\end{itemize}

\subsection{Arithmetic Logic Unit (ALU)}

The ALU performs arithmetic and logical operations. Table~\ref{tab:alu_ops} shows all supported operations.

\begin{table}[h]
\centering
\caption{ALU Operations}
\label{tab:alu_ops}
\begin{tabular}{ll}
\toprule
\textbf{ALU Op} & \textbf{Operation} \\
\midrule
0000 & ADD (rs1 + rs2/immediate) \\
0001 & SUB (rs1 - rs2) \\
0010 & AND (rs1 \& rs2/immediate) \\
0011 & OR (rs1 | rs2/immediate) \\
0100 & XOR (rs1 \textasciicircum rs2/immediate) \\
0101 & SLL (rs1 << shift\_amount) \\
0110 & SRL (rs1 >> shift\_amount) \\
0111 & SRA (rs1 >>> shift\_amount, arithmetic) \\
1000 & SLT (rs1 < rs2/immediate, signed) \\
1001 & SLTU (rs1 < rs2/immediate, unsigned) \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Branch Comparison Unit}

Branch conditions are evaluated in the datapath:

\begin{itemize}
    \item \textbf{BEQ}: rs1 == rs2
    \item \textbf{BNE}: rs1 != rs2
    \item \textbf{BLT}: rs1 < rs2 (signed)
    \item \textbf{BGE}: rs1 >= rs2 (signed)
    \item \textbf{BLTU}: rs1 < rs2 (unsigned)
    \item \textbf{BGEU}: rs1 >= rs2 (unsigned)
\end{itemize}

The branch target is calculated as PC + immediate (B-type immediate has LSB=0).

\subsection{Data Memory (DMEM)}

The data memory supports byte, half-word, and word accesses:

\begin{itemize}
    \item \textbf{Read Operations}:
    \begin{itemize}
        \item LB/LBU: Read byte, sign-extend (LB) or zero-extend (LBU)
        \item LH/LHU: Read half-word, sign-extend (LH) or zero-extend (LHU)
        \item LW: Read word
    \end{itemize}
    \item \textbf{Write Operations}:
    \begin{itemize}
        \item SB: Write byte (lowest byte of rs2)
        \item SH: Write half-word (lowest half-word of rs2)
        \item SW: Write word (full rs2)
    \end{itemize}
    \item \textbf{Byte Ordering}: Little-endian (LSB at lower address)
    \item \textbf{Misaligned Access}: Addresses truncated to appropriate boundary
\end{itemize}

\subsection{Load-Store Unit (LSU)}

The LSU manages data memory and I/O access:

\begin{itemize}
    \item \textbf{Address Decode}: Determines if address is in DMEM region or I/O region
    \item \textbf{Memory Access}: Routes read/write to DMEM or I/O registers
    \item \textbf{I/O Handling}: 
    \begin{itemize}
        \item Reads from switches, returns LED/LCD/HEX register values
        \item Writes to LED/LCD/HEX registers based on address
    \end{itemize}
\end{itemize}

\subsection{Writeback Mux}

The writeback multiplexer selects data for register write:

\begin{itemize}
    \item \textbf{00}: ALU result (for arithmetic/logical operations)
    \item \textbf{01}: Memory/I/O data (for load operations)
    \item \textbf{10}: PC + 4 (for JAL/JALR)
\end{itemize}

\section{Control Unit}

The control unit decodes instructions and generates control signals. The control flow varies based on instruction type, with different control signal combinations for R-type, I-type, S-type, B-type, U-type, and J-type instructions.

\subsection{Control Signal Generation}

For each instruction type, the control unit sets:

\begin{itemize}
    \item Register write enable (\texttt{reg\_write})
    \item Memory read/write enables (\texttt{mem\_read}, \texttt{mem\_write})
    \item ALU source selectors (\texttt{alu\_src\_a}, \texttt{alu\_src\_b})
    \item ALU operation code (\texttt{alu\_op})
    \item Memory-to-register selector (\texttt{mem\_to\_reg})
    \item Branch and jump flags (\texttt{branch}, \texttt{jump})
    \item Memory access size (\texttt{mem\_size})
\end{itemize}

\subsection{Instruction Types}

\subsubsection{R-type Instructions}
\begin{itemize}
    \item Source operands: rs1 and rs2 from register file
    \item ALU operation determined by funct3 and funct7
    \item Result written to register rd
\end{itemize}

\subsubsection{I-type Instructions}
\begin{itemize}
    \item Source operand: rs1 from register file
    \item Second operand: immediate value
    \item ALU operation determined by funct3 (and funct7 for shifts)
    \item Result written to register rd
\end{itemize}

\subsubsection{S-type Instructions}
\begin{itemize}
    \item Source operands: rs1 (base address) and rs2 (data to store)
    \item ALU calculates effective address: rs1 + immediate
    \item Data written to memory at calculated address
\end{itemize}

\subsubsection{B-type Instructions}
\begin{itemize}
    \item Source operands: rs1 and rs2 for comparison
    \item Branch target: PC + immediate
    \item PC update: branch target if condition true, else PC + 4
\end{itemize}

\subsubsection{U-type Instructions}
\begin{itemize}
    \item \textbf{LUI}: Load immediate upper 20 bits, lower 12 bits = 0
    \item \textbf{AUIPC}: Add PC to immediate upper 20 bits
\end{itemize}

\subsubsection{J-type Instructions}
\begin{itemize}
    \item \textbf{JAL}: Jump target is PC + immediate (computed in datapath), save PC + 4 to rd
    \item \textbf{JALR}: Jump target is rs1\_data + immediate (computed directly from register file output, not through ALU result), save PC + 4 to rd. The control unit sets ALU sources for other purposes, but the actual jump address uses \texttt{rs1\_data + imm} directly.
\end{itemize}

\section{Timing}

In a single-cycle processor, all operations complete within one clock cycle:

\begin{enumerate}
    \item \textbf{Fetch}: Instruction read from IMEM
    \item \textbf{Decode}: Instruction decoded, registers read, immediate generated
    \item \textbf{Execute}: ALU performs operation or address calculation
    \item \textbf{Memory}: Data memory/I/O accessed (if needed)
    \item \textbf{Writeback}: Result written to register file (if needed)
    \item \textbf{PC Update}: Next PC calculated and registered on clock edge
\end{enumerate}

All combinational operations (ALU, memory read, register read) complete within the clock period, and register/memory writes occur on the clock edge.

